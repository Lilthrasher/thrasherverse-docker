services:
  fileflows:
    image: revenz/fileflows:latest
    container_name: fileflows
    restart: unless-stopped
    ports:
      - ${PORT}:5000
    devices:
      - /dev/dri:/dev/dri
    environment:
      - TZ=${TZ}
      - PUID=${PUID}
      - PGID=${PGID}
    volumes:
      - ${TRANSCODE_DIR}:/temp
      - ${MEDIA_DIR}:/media
      - fileflows:/app/Data
      - fileflows:/app/Logs

volumes:
  fileflows:
